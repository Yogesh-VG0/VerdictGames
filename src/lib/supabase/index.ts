/**
 * VERDICT.GAMES — Supabase barrel export
 */

export { getServerSupabase } from "./server";
export { getBrowserSupabase } from "./client";
export type { Database, GameRow, GameInsert, ReviewRow, ListRow, ListItemRow, ProfileRow, GameSourceRow } from "./types";
